# MongoDB
# Obtén tu connection string desde MongoDB Atlas (https://www.mongodb.com/cloud/atlas)
# Ejemplo: mongodb+srv://usuario:password@cluster.mongodb.net/menu-digital?retryWrites=true&w=majority
MONGODB_URI=

# JWT Secret (genera una clave aleatoria segura de al menos 32 caracteres)
# Puedes generarla con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# Configuración del Restaurante (Opcional - se puede cambiar desde el panel admin)
NEXT_PUBLIC_RESTAURANT_NAME=Mi Restaurante
NEXT_PUBLIC_WHATSAPP_NUMBER=51999999999
